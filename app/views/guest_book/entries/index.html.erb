<h1>Listing guest_book_entries</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @guest_book_entries.each do |guest_book_entry| %>
  <tr>
    <td><%= link_to 'Show', guest_book_entry %></td>
    <td><%= link_to 'Edit', edit_guest_book_entry_path(guest_book_entry) %></td>
    <td><%= link_to 'Destroy', guest_book_entry, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Entry', new_guest_book_entry_path %>
